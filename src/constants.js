const PORT = '6882'
const ROOT =
      location.href.indexOf('localhost') > 0 ? 'http://localhost:' : 'http://reuvenliran.hopto.org:'
export const ROOT_URL = ROOT + PORT + '/'
export const ROOT_URL_API = ROOT_URL + 'api'
export const DEEP_ORANGE = '#FF5722'
